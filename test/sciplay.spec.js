import chai from 'chai';
import sciplay from '../src/sciplay.js';

chai.expect();
const expect = chai.expect;

describe('sciplay.js', () => {
    it('should exist', () => {
        expect(sciplay).to.exist;
    });
});
